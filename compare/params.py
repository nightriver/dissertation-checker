"""Єдине місце стартових порогів алгоритму з розділу 6.3 плану."""

FINGERPRINT_K = 5
WINNOW_WINDOW = 4
MAX_FINGERPRINT_POSTINGS = 40
MAX_SEED_GAP = 60
MAX_CHAIN_DRIFT = 120
MIN_SEEDS_PER_CHAIN = 2
MIN_CANDIDATE_TOKENS = 15
MAX_CANDIDATE_TOKENS = 3000
CANDIDATE_OVERLAP = 200
MAX_CHAINS = 5000
CANDIDATE_MERGE_GAP = 5

FUZZY_THRESHOLD = 0.75
FUZZY_SHORT_THRESHOLD = 0.70

MIN_MATCHED_LOW = 25
MIN_SIMILARITY = 0.45
MIN_VERBATIM_LOW = 15
MIN_MATCHED_HIGH = 40
MIN_VERBATIM_HIGH = 30

# Максимум слів поспіль без збігу всередині одного рядка таблиці.
# Ширший розрив означає два різні фрагменти, і показувати їх треба двома
# рядками: інакше в правій комірці опиняється текст, якого немає в лівій.
# Число 15 — заміряне (PLAN_COMPARE_FIX.md, крок 1): покриття не змінюється
# ані на десяту частку відсотка, а найгірший рядок піднімається з 0,41 до
# 0,60 за схожістю. Строго жорсткіше за MAX_SEED_GAP = 60: той обмежує
# розрив між відбитками при побудові ланцюга, цей — між збігами в готовому
# рядку.
MAX_MATCH_GAP = 15

# Частка сегмента, яку має покривати збіг з кожного боку окремо.
# Страхує від рядка, де збіг є, але потонув у чужому тексті.
MIN_SEGMENT_COVERAGE = 0.5

# Частка перекриття, після якої знахідка вважається повтором уже залишеної.
# Перевіряється по кожному боку ОКРЕМО: один абзац ліворуч і п'ять його
# копій праворуч — це один рядок з лічильником, а не п'ять рядків.
DUPLICATE_OVERLAP = 0.8

# Відхилення дрейфу (b_start - a_start) від медіани, після якого знахідка
# рахується такою, що стоїть осторонь основного відповідання документів.
# Тільки для показника над таблицею; нічого не відсіює.
OFF_ALIGNMENT_DRIFT = 2000

# Розділ 6.7: лишається вимкненим, доки немає відомих пар для калібрування.
USE_TRUNCATED_STEMS = False
TRUNCATED_STEM_LENGTH = 6
