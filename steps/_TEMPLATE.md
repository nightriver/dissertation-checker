# Шаг N — <суть>

> Пакет шага. Самодостаточен: сабагенты работают **только по нему** и не
> открывают `PLAN_SEARCH.md`. Пишет оркестратор, правит только оркестратор.
> Если чего-то не хватает — это дефект пакета, а не повод читать план.

## Что делаем

<5–10 строк своими словами: что появится в продукте после этого шага.>

## Файлы шага

**Создать:**
- `path/to/new.py` — <за что отвечает>

**Изменить:**
- `path/to/existing.py` — <что именно меняется>

**Не трогать:**
- <файлы соседних шагов волны: «их пишет параллельный агент»>
- <модули, которые остаются каркасом до шага M>

## Контракт

Публичные имена, которые обязан предоставить исполнитель и на которые
опирается шлюз. Меняются только через оркестратора.

```python
def имя(аргумент: Тип, *, today: date) -> ТипРезультата: ...
```

<Для каждого — одна строка: что возвращает, что делает при пустом входе.>

## Числа

Все до одного. Число, которого здесь нет, исполнитель придумывать не имеет
права — он обязан вернуть вопрос (`CLAUDE.md` №8).

| Константа | Значение | Источник |
|---|---|---|
| `WINDOW_MIN_WORDS` | 6 | §13, шаг 1 |

## Шлюз

Пункты, каждый — отдельный тест в `tests/test_gate_step_NN.py`.

1. <проверяемое утверждение с конкретным входом и конкретным ожиданием>
2. Детерминизм: два прогона подряд дают тот же результат и тот же порядок.

## Отказы

Граничные случаи и ожидаемое поведение. У каждого отсева — своя причина в
диагностике, молчаливого подавления нет (`CLAUDE.md` №3).

| Вход | Ожидание | Причина отсева |
|---|---|---|
| <…> | <…> | `query_too_long` |

## Вне объёма

Что относится к будущим шагам и остаётся каркасом с docstring:

- <…> — шаг M

## Выписки из плана

Дословные цитаты нужных абзацев `PLAN_SEARCH.md`, с номерами секций и строк.
Ровно столько, чтобы исполнитель и `gate-writer` не открывали план.

> §NN, строки A–B:
> <цитата>
